# 深度学习框架 (请根据CUDA版本选择正确的PyTorch安装命令)
# 对于CUDA 11.8:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 对于CUDA 12.1:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 科学计算库
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0

# 可视化
matplotlib==3.7.2
seaborn==0.12.2

# 进度条和工具
tqdm==4.65.0

# 中国股市数据
akshare==1.11.80
baostock==0.8.8

# GPU内存监控 (可选但推荐)
pynvml==11.5.0
psutil==5.9.5

# 大数据处理支持
h5py==3.9.0

# 其他工具
argparse  # Python内置，无需安装
json      # Python内置，无需安装
datetime  # Python内置，无需安装
logging   # Python内置，无需安装
gc        # Python内置，无需安装
time      # Python内置，无需安装
os        # Python内置，无需安装